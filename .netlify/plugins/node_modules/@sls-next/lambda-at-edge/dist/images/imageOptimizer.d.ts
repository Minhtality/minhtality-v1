/// <reference types="node" />
import { UrlWithParsedQuery } from "url";
import { IncomingMessage, ServerResponse } from "http";
import { ImagesManifest } from "../types";
export declare function imageOptimizer({ basePath, bucketName, region }: {
    basePath: string;
    bucketName: string;
    region: string;
}, imagesManifest: ImagesManifest | undefined, req: IncomingMessage, res: ServerResponse, parsedUrl: UrlWithParsedQuery): Promise<{
    finished: boolean;
}>;
export declare function getMaxAge(str: string | null): number;
