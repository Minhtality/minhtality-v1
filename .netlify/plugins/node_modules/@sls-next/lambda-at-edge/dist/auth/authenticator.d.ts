import { CloudFrontResultResponse } from "aws-lambda";
export declare function getUnauthenticatedResponse(authorizationHeader: string | null, authentication: {
    username: string;
    password: string;
} | undefined): CloudFrontResultResponse | null;
