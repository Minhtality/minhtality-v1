import React, { useState } from 'react';
import * as Styled from '../styles/hero.styled';

export const Hero = () => {
    return (
        <Styled.Page>

        </Styled.Page>
    )
};

export default Hero;